[project]
name = "local-whisper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faster-whisper",
    "torch>=2.0.0",
    "fastapi",
    "uvicorn",
    "python-multipart",
]
